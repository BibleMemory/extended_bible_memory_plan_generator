{
  "name": "bible-memory-plan-generator",
  "version": "1.5.0",
  "description": "Bible memory plan generator widget for Ghost 6 blogs — day-by-day schedules for memorizing whole books of the Bible",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "vitest": "^2.1.0"
  }
}
